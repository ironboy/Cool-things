// not much here so far, but we would put the logic 
// for buying a product here eventually
export default function makeBuy({ id, name }) {
  alert(`You're about the buy a ${name.toLowerCase()}!`);
}