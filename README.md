* Can you hack this site via file uploads?
* The goal is that you can upload a script (in this particular case a JavaScript) and make it run.
* **Note:** Normally visiting an url with a JavaScript does not run the JavaScript on the backend but in this case it will! (Thus emulating the standard behavior of web servers like Nginx and Apache when it comes to php scripts!)